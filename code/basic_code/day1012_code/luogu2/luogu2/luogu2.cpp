#include<iostream>
#include<algorithm>
using namespace std;
int main() {
    int n = 0, m = 0;
    int y[2002];
    int x[2002];
    int priceY = 0;priceX = 0
        for (int i = 0;i < n - 1;i++) {
            cin >> y[i]
        }
    for (int i = 0;i < m - 1;i++) {
        cin >> x[i]
    }
    sort(y, y + n - 1);
    sort(x, x + n - 1);



    return 0;
}